.header {
	padding-top: 40px;
	padding-bottom: 35px;
	z-index: 1;
	position: relative;

	.wrapper {
		position: relative;
		display: flex;
		flex-direction: row;
		justify-content: space-between;
	}
}
